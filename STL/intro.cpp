// -STL-standard template lib(in simple way: its provide a premade lib so you can focus on logic instead of making everything manually from scratch)
// - pre made tool kit for dsa

// -It has three pillar:
// -1.Container(store the data)
// -2.algorithm(work for data)
// -3.Iterator(Navigate through container)


// -Containers:
// -1.Vector->array with dynamic size
// -2.List->linked list with dynamic size
// -3.Set->unordered set of unique elements
// -4.Map ->unordered map of key-value pairs

// -Algorithms:
// -1.Find->search for an element
// -2.Sort ->sort the elements

// -Iterators: ->used to navigate through containers
// -1.Begin ->points to first element
// -2.End ->points to last element

// -Utility:
// -1.Pair

// -Iterators:
// -1.Begin ->points to first element
// -2.End ->points to last element
